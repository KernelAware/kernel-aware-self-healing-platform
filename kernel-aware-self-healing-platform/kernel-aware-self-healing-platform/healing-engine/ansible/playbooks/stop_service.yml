---
- name: Stop service
  hosts: "{{ target_host }}"
  become: true
  gather_facts: false
  tasks:
    - name: Stop requested service
      ansible.builtin.service:
        name: "{{ service_name }}"
        state: stopped
