---
- name: Restart service
  hosts: "{{ target_host }}"
  become: true
  gather_facts: false
  tasks:
    - name: Restart requested service
      ansible.builtin.service:
        name: "{{ service_name }}"
        state: restarted
